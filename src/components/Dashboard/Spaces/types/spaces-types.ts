export type Event = {
  name: string
  date: string //repalce with Date
}

export type Topic = {
  name: string
  posts: number
}

export type Stat = {
  name: string
  amount: number
  icon: React.ReactNode
}
